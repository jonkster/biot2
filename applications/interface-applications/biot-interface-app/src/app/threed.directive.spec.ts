import { ThreedDirective } from './threed.directive';

describe('ThreedDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreedDirective();
    expect(directive).toBeTruthy();
  });
});
