import { ThreestatsDirective } from './threestats.directive';

describe('ThreestatsDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreestatsDirective();
    expect(directive).toBeTruthy();
  });
});
